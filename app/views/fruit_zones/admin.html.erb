<%= form_tag("upload", remote: true, :html => { :class => 'form-horizontal', :multipart => true }) do %>
  <%= file_field_tag :file_url, :class => 'text_field' %>
  <%= submit_tag "submit" %>
<% end %>
